version {{ os_version }};

system {
    host-name {{ inventory_hostname.split('.')[0] }};
    domain-name {{ inventory_hostname.split('.')[1] }}
}
protocols {
    lldp {
        interface all;
    }
}
